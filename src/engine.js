import { Vertex } from 'js_engine_boilerplate'

const vertex = new Vertex();

/* const apiBase = {
    test: {
        url: ''
    },
    development: {
        url: ''
    },
    production: {
        url: ''
    }
}

const env = import.meta.env.MODE;

vertex.updateSetting('env', env);
vertex.updateSetting('apiBase', apiBase[vertex.getSetting('env')].url); */

export { vertex as default, vertex }